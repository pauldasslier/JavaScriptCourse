/*let x = [];

function getFriendlyNumbers(start, end) {
	for(let i = 2; i <= end; i++) {
		x.push()
	}
    // return []
}
getFriendlyNumbers(1, 300);
*/

// Нужно взять число end и поделить его на числа от 1го до максимального числа,
// кроме него самого
let rest = end % i;
function getFriendlyNumbers(start, end) {
	for(let i = 1; i <= end; i++) {
		if (rest = 0) {}
	}
}